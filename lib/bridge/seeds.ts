// Decoded from bridge and base-relayer IDL constants.
// These must match the on-chain program seeds exactly.

export const BRIDGE_SEED = Buffer.from("bridge");
export const OUTGOING_MESSAGE_SEED = Buffer.from("outgoing_message");
export const TOKEN_VAULT_SEED = Buffer.from("token_vault");

export const CFG_SEED = Buffer.from("config");
export const MTR_SEED = Buffer.from("mtr");
